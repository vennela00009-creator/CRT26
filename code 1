class Solution(object):
  def lengthOfLongestSubstring(self,s):
   i=0
   res=0
   visited=set()
   for j in range(len(s)):
       while s[j] in visited:
        visited.remove(s[i])
        i+=1
    res=max(res,(j-i)+1)
    visited.add(s[i])
return res
